---
type: raw_capture
source_type: pasted
title: "Sunder sync: ai-agent-memory-patterns.json"
url: "https://blogs.oracle.com/developers/comparing-file-systems-and-databases-for-effective-ai-agent-memory-management"
collected_at: 2026-06-11T03:10:00+08:00
published_at: Unknown
capture_quality: complete
status: raw
trust_lane: intentional
source_file: "/Users/sethlim/Documents/Sunder Workspace/ai-agent-memory-patterns.json"
source_root: "/Users/sethlim/Documents/Sunder Workspace"
source_relpath: "ai-agent-memory-patterns.json"
sha256: "256c125838c5f4ca5f18adf6b7eda756fdc47396645defd61f12b6d2a171ddfa"
duplicate_of: ""
---

# Sunder sync: ai-agent-memory-patterns.json

Source file: `/Users/sethlim/Documents/Sunder Workspace/ai-agent-memory-patterns.json`

Primary URL: https://blogs.oracle.com/developers/comparing-file-systems-and-databases-for-effective-ai-agent-memory-management

Duplicate of existing source-map entry: `none`

## Capture Text

```json
{
  "search_id": "search_32fcce966ff640d69f5ec2ed765618df",
  "status": "ok",
  "results": [
    {
      "url": "https://blogs.oracle.com/developers/comparing-file-systems-and-databases-for-effective-ai-agent-memory-management",
      "title": "Comparing File Systems and Databases for Effective AI Agent Memory Management | developers",
      "publish_date": "2026-02-05",
      "excerpts": [
        "Section Title: Comparing File Systems and Databases for Effective AI Agent Memory Management > **Key Takeaways:**\nContent:\nAI developers are watching agent engineering evolve in real time, with leading teams openly sharing what works. One principle keeps showing up from the front lines: **build within the LLM\u2019s constraints** .\nIn practice, two constraints dominate:\n**LLMs are stateless across sessions** (no durable memory unless you bring it back in).\n**Context windows are bounded** (and performance can degrade as you stuff more tokens in).\nSo \u201cjust add more context\u201d isn\u2019t a reliable strategy due to the quadratic cost of attention mechanisms and the degradation of reasoning capabilities as context fills up. The winning pattern is **external memory + disciplined retrieval** : store state outside the prompt (artifacts, decisions, tool outputs), then pull back only what matters for the current loop.\nSection Title: Comparing File Systems and Databases for Effective AI Agent Memory Management > **Key Takeaways:**\nContent:\nThere\u2019s also a useful upside: because models are trained on internet-era developer workflows, they\u2019re unusually competent with **developer-native interfaces** repos, folders, markdown, logs, and CLI-style interactions. That\u2019s why filesystems keep showing up in modern agent stacks.\nThis is where the debate heats up: \u201cfiles are all you need\u201d for agent memory. Most arguments collapse because they treat **interface** , **storage** , and **deployment** as the same decision. They aren\u2019t.\n**Filesystems are winning as an interface** because models already know how to list directories, grep for patterns, read ranges, and write artifacts. **Databases are winning as a substrate** because once memory must be shared, audited, queried, and made reliable under concurrency, you either adopt database guarantees or painfully reinvent them.\n ... \nSection Title: ... > **What is Agent Memory?**\nContent:\n**Agent memory is the set of system components and techniques that enable an AI agent to store, recall, and update information over time so it can adapt to new inputs and maintain continuity across long-horizon tasks.** Core components typically include the language and embedding model, information retrieval mechanisms, and a persistent storage layer such as a database.\u200b\u200b\n**Types of Agent Memory**\nTypes of Agent Memory\nIn practical systems, agent memory is usually classified into two distinct forms:\n**Short-term memory(Working memory)** : whatever is currently inside the context window.\n**Long-term memory** : a persistent state that survives beyond a single call or session (facts, artifacts, plans, prior decisions, tool outputs).\n ... \nSection Title: Comparing File Systems and Databases for Effective AI Agent Memory Management > ... > Memory Manager\nContent:\ndef __init__(self, conn, conversation_table: str, knowledge_base_vs, summary_vs, tool_log_table):\n        self.conn = conn\n        self.conversation_table = conversation_table\n        self.knowledge_base_vs = knowledge_base_vs\n        self.summary_vs = summary_vs\n        self.tool_log_table = tool_log_table\n    \n    def write_conversational_memory(self, content: str, role: str, thread_id: str) -> str:\n        thread_id = str(thread_id)\n        with self.conn.cursor() as cur:\n            id_var = cur.var(str)\n            cur.execute(f\"\"\"\n                INSERT INTO {self.conversation_table} (thread_id, role, content, metadata, timestamp)\n                VALUES (:thread_id, :role, :content, :metadata, CURRENT_TIMESTAMP)\n                RETURNING id INTO :id\n            \"\"\", {\"thread_id\": thread_id, \"role\": role, \"content\": content, \"metadata\": \"{}\", \"id\": id_var})\n            record_id =\n ... \nSection Title: Comparing File Systems and Databases for Effective AI Agent Memory Management > ... > Memory Manager\nContent:\nself.conn.cursor() as cur:\n            cur.execute(f\"\"\"\n                UPDATE {self.conversation_table}\n                SET summary_id = :summary_id\n                WHERE thread_id = :thread_id AND summary_id IS NULL\n            \"\"\", {\"summary_id\": summary_id, \"thread_id\": thread_id})\n        self.conn.commit()\n        print(f\"  Marked messages as summarized (summary_id: {summary_id})\")\n    \n    def write_knowledge_base(self, text: str, metadata_json: str = \"{}\"):\n        metadata = json.loads(metadata_json)\n        self.knowledge_base_vs.add_texts([text], [metadata])\n\n    def read_knowledge_base(self, query: str, k: int = 5) -> str:\n        results = self.knowledge_base_vs.similarity_search(query, k=k)\n        content = \"\\n\".join([doc.page_content for doc in results])\n        return f\"\"\"## Knowledge Base Memory: This are general information that is relevant to the question\n### How to\n ... \nSection Title: ... > Creating the tools and agent\nContent:\nThe database-first agent follows a simple, production-friendly pattern.\nPersists every conversation turn as structured rows, including user and assistant messages with thread or run IDs and timestamps, so sessions are recoverable, traceable, and consistent across restarts.\nPersists long-term knowledge in a vector-enabled store by chunking documents, generating embeddings, and storing them with metadata, so retrieval is semantic, ranked, and fast as the corpus grows.\nPersists tool activity as first-class records that capture the tool name, inputs, outputs, status, errors, and key metadata, so agent behavior is inspectable, reproducible, and auditable.\nOn top of that, the agent actively manages context: it tracks token usage and periodically rolls older dialogue and intermediate state into durable summaries (and/or \u201cmemory\u201d tables), so the working prompt stays small while the full history remains available on demand.\n ... \nSection Title: ... > Frequently Asked Questions\nContent:\n**What is AI Agent memory? ** AI agent memory is the set of system components and techniques that enable an AI agent to store, recall, and update information over time. Because LLMs are inherently stateless\u2014they have no built-in ability to remember previous sessions\u2014agent memory provides the persistence layer that allows agents to maintain continuity across conversations, learn from past interactions, and adapt to user preferences. **Should I use a filesystem or a database for an AI agent\u2019s memory? ** It depends on your use case. Filesystems excel at single-user prototypes, artifact-heavy workflows, and rapid iteration\u2014they\u2019re simple, transparent, and align with how LLMs naturally operate. Databases become essential when you need concurrent access, ACID transactions, semantic retrieval, or shared state across multiple agents or users."
      ]
    },
    {
      "url": "https://www.tigerdata.com/learn/building-ai-agents-with-persistent-memory-a-unified-database-approach",
      "title": "Building AI Agents with Persistent Memory: A Unified Database Approach | Tiger Data",
      "publish_date": "2026-01-20",
      "excerpts": [
        "Database: Understanding the Connection](/learn/vector-store-vs-vector-database) [Pgvector vs. Pinecone: Vector Database Performance and Cost Comparison](/learn/pgvector-vs-pinecone) [How to Build LLM Applications With Pgvector Vector Store in LangChain](/learn/how-to-build-llm-applications-with-pgvector-vector-store-in-langchain) [How to Implement RAG With Amazon Bedrock and LangChain](/learn/how-to-implement-rag-with-amazon-bedrock-and-langchain) [Retrieval-Augmented Generation With Claude Sonnet 3.5 and Pgvector](/learn/retrieval-augmented-generation-with-claude-sonnet-3-5-and-pgvector) [RAG Is More Than Just Vector Search](/learn/rag-is-more-than-just-vector-search) [PostgreSQL Hybrid Search Using Pgvector and Cohere](/learn/postgresql-hybrid-search-using-pgvector-and-cohere) [Implementing Filtered Semantic Search Using Pgvector and JavaScript](/learn/implementing-filtered-semantic-search-using-pgvector-and-javascript-2) [Refining Vector Search Queries With Time Filters in\n ... \n[Postgres cheat sheet](/learn/postgres-cheat-sheet)\n[Home](/developers) [Time series basics](/learn/time-series-basics) [Postgres basics](/learn/postgres-basics) [Postgres guides](/learn/postgres-guides) [Postgres best practices](/learn/postgres-best-practices) [Postgres extensions](/learn/postgres-extensions) [Postgres for real-time analytics](/learn/postgres-for-real-time-analytics)\n[Sections](/learn/postgres-and-vector-data)\nTutorials\n[How to Build LLM Applications With Pgvector Vector Store in LangChain](/blog/how-to-build-llm-applications-with-pgvector-vector-store-in-langchain) [How to Implement RAG With Amazon Bedrock and LangChain](/blog/how-to-implement-rag-with-amazon-bedrock-and-langchain) [Retrieval-Augmented Generation With Claude Sonnet 3.5 and Pgvector](/learn/retrieval-augmented-generation-with-claude-sonnet-3-5-and-pgvector) [RAG Is More Than Just Vector Search](/blog/rag-is-more-than-just-vector-search)\nHybrid search & filtering\n ... \nSection Title: ... > The Three Types of Agent Memory\nContent:\n**Procedural memory** stores structured preferences and learned behaviors in relational tables. When agents remember \u201cthis user prefers Python\u201d or \u201calways format code as TypeScript\u201d, that\u2019s procedural memory requiring transactional updates with foreign key constraints.\nSection Title: ... > The Three Types of Agent Memory\nContent:\n| Memory Type | Data Pattern | Query Type | PostgreSQL Implementation |\n| Episodic | Time-series events | Temporal range queries | Hypertables with automatic partitioning |\n| Semantic | Vector embeddings | K-nearest neighbor search | pgvector columns with DiskANN indexes |\n| Procedural | Relational data | CRUD with joins | Standard PostgreSQL tables |\n ... \nSection Title: ... > Schema Design: Conversation Store (Episodic Memory)\nContent:\nThe `create_hypertable` call partitions the table into 7-day chunks. When querying recent messages, PostgreSQL scans only relevant chunks. As data ages, Tiger Data compresses old chunks at 10:1 ratios, keeping storage manageable without sacrificing query performance.\n ... \nSection Title: ... > Schema design: Agent State (Procedural Memory)\nContent:\n`-- Procedural memory: user preferences and agent configuration CREATE TABLE users ( id UUID PRIMARY KEY DEFAULT gen_random_uuid(), email TEXT UNIQUE NOT NULL, preferences JSONB DEFAULT '{}', created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), last_active_at TIMESTAMPTZ NOT NULL DEFAULT NOW() ); CREATE TABLE agents ( id UUID PRIMARY KEY DEFAULT gen_random_uuid(), name TEXT NOT NULL, system_prompt TEXT NOT NULL, learned_strategies JSONB DEFAULT '{}', avg_response_time_ms INTEGER, total_conversations INTEGER DEFAULT 0, created_at TIMESTAMPTZ NOT NULL DEFAULT NOW() ); CREATE TABLE user_agents ( user_id UUID REFERENCES users(id) ON DELETE CASCADE, agent_id UUID REFERENCES agents(id) ON DELETE CASCADE, is_default BOOLEAN DEFAULT FALSE, PRIMARY KEY (user_id, agent_id) );` Copy Relational structure supports transactional updates like \u201cupdate user preferences and default agent atomically\u201d.\n ... \nSection Title: ... > Context window construction: Single-query pattern\nContent:\ndistance ASC LIMIT 5 ), user_prefs AS ( SELECT preferences FROM users WHERE id = $3 ) SELECT jsonb_build_object( 'messages', ( SELECT jsonb_agg( jsonb_build_object( 'role', role, 'content', content, 'created_at', created_at, 'estimated_tokens', estimated_tokens ) ORDER BY created_at ASC -- Maintain order ) FROM recent_messages ), 'facts', ( SELECT jsonb_agg( jsonb_build_object( 'content', content, 'source', source_url, 'distance', distance ) ORDER BY distance ASC ) FROM relevant_knowledge ), 'preferences', (SELECT preferences FROM user_prefs) ) AS context `, [ conversationId, `[${queryEmbedding.join(',')}]`, userId, queryText // Full-text search parameter ]); const context = result.rows[0]?.context || {}; // Token budget enforcement let totalTokens = 0; const messages = (context.messages || []).filter((msg: any) => { totalTokens += msg.estimated_tokens || 0; return totalTokens <= maxTokens * 0.8; // Reserve 20% for facts/system\nSection Title: ... > Context window construction: Single-query pattern\nContent:\nprompt }); return { recentMessages: messages, relevantFacts: context.facts || [], userPreferences: context.preferences || {}, tokenCount: totalTokens }; }` Copy One database query constructs the complete context window. CTEs execute in parallel (PostgreSQL\u2019s query planner handles this), and the final SELECT combines everything into JSON. One network round trip, one transaction, consistent point-in-time snapshot across all memory types.\n ... \nSection Title: LangGraph checkpointer for agent state persistence > Production & Performance\nContent:\nDiskANN achieves [28x lower p95 latency](https://www.tigerdata.com/blog/pgvector-is-now-as-fast-as-pinecone-at-75-less-cost#:~:text=28x%20lower%20p95%20latency) than Pinecone\u2019s s1 index at 99% recall, but choose based on your scale: HNSW for <100M vectors fitting in RAM, DiskANN for billion-scale or cost-sensitive deployments. Compression policies run automatically in the background, reducing storage by 10:1 without blocking writes."
      ]
    },
    {
      "url": "https://medium.com/@nomannayeem/building-ai-agents-that-actually-remember-a-developers-guide-to-memory-management-in-2025-062fd0be80a1",
      "title": "Building AI Agents That Actually Remember: A Developer\u2019s Guide to Memory Management in 2025 | by Nayeem Islam | Medium",
      "publish_date": "2025-06-13",
      "excerpts": [
        "Section Title: ... > Long-Term Memory: The Personal Filing Cabinet\nContent:\nThis is where things get interesting. Long-term memory lets your AI remember users across different sessions, learn their preferences, and build genuine relationships over time.\nWhen Alex comes back next week, your bot should remember their name, their previous questions, and maybe even that they mentioned loving pizza during your last chat.\n ... \nSection Title: ... > Making Entity Memory Persistent\nContent:\njson.dump(self.langchain_memory.entity_store, f, indent=2)  \n  \n    def remember_user(self, user_id: str, conversation_text: str):  \n        \"\"\"Process and remember information about a user\"\"\"  \n        # Extract entities from conversation  \n        entities = self.langchain_memory.llm.predict(  \n            f\"Extract key information about people, companies, and preferences from: {conversation_text}\"  \n        )  \n  \n        # Store user-specific information  \n        if user_id not in self.entities:  \n            self.entities[user_id] = {}  \n  \n        # Update entity information  \n        self.entities[user_id].update({  \n            \"last_conversation\": conversation_text[:200],  # Keep snippet  \n            \"last_seen\": self.get_current_timestamp(),  \n            \"conversation_count\": self.entities[user_id].get(\"conversation_count\", 0) + 1  \n        })  \n  \n        # Save to persistent storage  \n        self.save_entities()  \n  \n    def\n ... \nSection Title: ... > Multi-Modal Memory: Beyond Just Text\nContent:\nPreferences:  \n        - Prefers {patterns['preferred_response_length']} length responses  \n        - Typically engages for {patterns['typical_session_duration']:.1f} minutes  \n        - Common topics: {', '.join(patterns['common_topics'][:5])}  \n        - Complexity level: {patterns['complexity_preference']}  \n        \"\"\"  \n  \n        return context.strip()  \n  \n# Usage with pattern tracking  \nadvanced_memory = AdvancedMemorySystem()  \n  \nconversation = ConversationChain(  \n    llm=OpenAI(temperature=0.7),  \n    memory=advanced_memory.combined_memory,  \n    verbose=True  \n)  \n  \n# Simulate a conversation with pattern tracking  \nuser_id = \"sarah_123\"  \n  \n# Track that Sarah prefers detailed responses  \nadvanced_memory.track_interaction_pattern(user_id, {  \n    'response_length_preference': 'detailed',  \n    'session_duration': 15.5,  \n    'topics': ['machine learning', 'data science', 'python'],  \n    'complexity_preference': 'advanced'  \n})\n ... \nSection Title: ... > The Power of Advanced Memory\nContent:\nWhen you implement these advanced memory patterns, you\u2019re not just building a better chatbot, you\u2019re creating an AI system that genuinely learns and adapts. Users notice the difference immediately:\n**Traditional Memory:**\n\u201cWhat did we discuss last time?\u201d\nSearches through conversation history\nReturns basic information\n**Advanced Memory:**\nRemembers Sarah prefers detailed technical explanations\nKnows she\u2019s working on machine learning projects\nUnderstands her expertise level from interaction patterns\nAdapts communication style automatically\nProactively mentions relevant past topics\nThis is the difference between AI that serves and AI that understands.\n ... \nSection Title: ... > LangChain\u2019s Multi-Agent Memory Architecture\nContent:\nPreferences learned by other agents  \n        if user_data.get('preferences'):  \n            context_parts.append(\"\\nUser preferences (learned by team):\")  \n            for pref_type, pref_value in user_data['preferences'].items():  \n                context_parts.append(f\"  - {pref_type}: {pref_value}\")  \n  \n        # Issues or patterns noticed by other agents  \n        if user_data.get('issues'):  \n            context_parts.append(f\"\\nKnown issues: {len(user_data['issues'])} previous issues logged\")  \n  \n        return \"\\n\".join(context_parts)  \n  \n    def share_learning(self, user_id: str, agent_id: str, learning: Dict[str, Any]):  \n        \"\"\"Share what an agent learned about a user with the team\"\"\"  \n        if user_id not in self.user_memories:  \n            self.user_memories[user_id] = {'preferences': {}, 'issues': []}  \n  \n        # Update shared preferences  \n        if 'preferences' in learning:\nSection Title: ... > LangChain\u2019s Multi-Agent Memory Architecture\nContent:\nself.user_memories[user_id]['preferences'].update(learning['preferences'])  \n  \n        # Log issues for team awareness  \n        if 'issue' in learning:  \n            self.user_memories[user_id]['issues'].append({  \n                'reported_by': agent_id,  \n                'issue': learning['issue'],  \n                'timestamp': datetime.now().isoformat()  \n            })  \n  \nclass TeamAwareAgent:  \n    \"\"\"An agent that participates in shared memory\"\"\"  \n  \n    def __init__(self, agent_id: str, role: str, shared_memory: SharedMemoryStore):  \n        self.agent_id = agent_id  \n        self.role = role  \n        self.shared_memory = shared_memory  \n  \n        # Individual agent memory  \n        self.local_memory = ConversationSummaryBufferMemory(  \n            llm=OpenAI(temperature=0),  \n            max_token_limit=1000,  \n            return_messages=True  \n        )  \n  \n        # Conversation chain with role-specific prompt\n ... \nSection Title: ... > Production-Grade Memory Architecture\nContent:\nconnection  \n            await self._test_redis_connection()  \n  \n            # Initialize database  \n            await self._initialize_database()  \n  \n            self.logger.info(\"Production memory store initialized successfully\")  \n  \n        except Exception as e:  \n            self.logger.error(f\"Failed to initialize memory store: {e}\")  \n            raise  \n  \n    async def _test_redis_connection(self):  \n        \"\"\"Test Redis connection with fallback handling\"\"\"  \n        try:  \n            await self.redis_client.ping()  \n        except redis.ConnectionError:  \n            self.logger.warning(\"Redis connection failed, running without cache\")  \n            self.redis_client = None  \n  \n    async def _initialize_database(self):  \n        \"\"\"Initialize database with proper schema\"\"\"  \n        # In production, you'd use proper database connection pooling  \n        # This is a simplified example  \n  \n        schema = \"\"\"  \n        CREATE"
      ]
    },
    {
      "url": "https://www.reddit.com/r/ChatGPTCoding/comments/1pqhly1/agentsdb_an_agentsmd_alternative_for_llm_context/",
      "title": "AGENTS.db - an AGENTS.md alternative for LLM context - Reddit",
      "publish_date": null,
      "excerpts": [
        "Dec 19, 2025 \u00b7 I built AGENTS.db which keeps the spirit of AGENTS.md while scaling it into a layered, append\u2011only, vectorized flatfile database for LLM agents. I gave an AI agent persistent memory using just markdown files Breaking the Context Window: Building Infinite Memory for AI Agents More results from www.reddit.com Missing: memory | Show results with: memory"
      ]
    },
    {
      "url": "https://medium.com/@chetankerhalkar/agentic-memory-patterns-context-engineering-the-real-os-of-ai-agents-614cf0cf98b3",
      "title": "Agentic Memory Patterns & Context Engineering \u2014 The Real OS of AI Agents | by Chetankerhalkar | Dec, 2025 | Medium",
      "publish_date": "2025-12-05",
      "excerpts": [
        "Section Title: ... > 1. From Prompts to Agentic Memory: Why This Matters Now\nContent:\nRecent work on agentic workflows and LLM agents emphasizes that memory is not optional anymore: it\u2019s the backbone that lets agents plan, adapt, and improve over long horizons.\nAt a high level:\nModels provide reasoning and language.\nTools provide actions.\nMemory + Context provide *continuity* and *self-improvement* .\nCloud providers and research papers are converging on the same idea:\n*A basic building block of agentic workflows is the ability to augment an LLM\u2019s context with data, tools, and memory in a dynamic reasoning loop.*\nAt the same time, context engineering has evolved into a formal discipline: the systematic design of what goes into the model\u2019s context window (instructions, retrieved data, examples, state) to maximize reliability and performance, under hard token and time budgets.\nSo when we say \u201cagentic memory patterns\u201d, we\u2019re really talking about:\n ... \nSection Title: ... > 3. What Is Context Engineering (in Practice)?\nContent:\nContext engineering is the discipline of designing, structuring, and optimizing the information we feed into a model:\nIt goes beyond \u201cprompt engineering\u201d (which mostly focuses on *instructions* ).\nIt includes what to retrieve, how to compress, how to structure, and how to evolve the context over time.\nThink of it as:\n*\u201cInformation architecture for an LLM\u2019s brain.\u201d*\nKey ingredients (from recent surveys and platform docs):\nPrompts & System Messages \u2014 core instructions, roles, policies.\nMemory \u2014 episodic, semantic, and procedural content.\nRAG / Search \u2014 retrieving relevant knowledge into the context.\nTools & Functions \u2014 ways to act and gather fresh state.\nStructured Outputs & Schemas \u2014 how the model\u2019s outputs are shaped for reuse.\nAgentic memory patterns sit right in the middle: they\u2019re the repeatable ways you design and evolve this context over time.\n ... \nSection Title: ... > Pattern 1 \u2014 Context Framing & Role Scaffolding\nContent:\nWhat it is Designing a *stable* system prompt + initial context \u201cframe\u201d that defines:\nThe agent\u2019s role and boundaries\nTools it can use\nHow it should interpret memory and retrieved data\nOutput formats and self-check routines\nWhy it matters This is the anchor for procedural memory: your agent\u2019s \u201cjob description\u201d + SOP, applied every single turn.\nDay-to-day usage\nCustomer support agent:\nSystem prompt includes persona, allowed actions, escalation rules, PII rules.Memory is introduced with labels: `UserProfile` , `RecentTickets` , `OrgPolicies` .\nCoding assistant:\nSystem prompt includes repo conventions, testing practices, \u201cnever commit secrets\u201d, etc.Memory includes \u201cHistory of recently edited files\u201d and \u201cKnown failure modes\u201d as structured snippets.\n ... \nSection Title: ... > Pattern 5 \u2014 Profile & Preference Store\nContent:\nWhat it is Persistent user or org profile that encodes stable preferences and constraints, separate from conversations.\nThis is *structured semantic memory* about the user.\nDay-to-day usage\nPersonal assistant agent: Store work hours, meeting preferences, favorite tools, \u201calways CC these people\u201d, etc.\nCoding agent: Store preferred languages, frameworks, code style, risk tolerance (\u201cprefer safe refactors over aggressive performance hacks\u201d).\nImplementation sketch\nExample JSON:\n```\n{  \n\"user_id\" : \"user-123\" ,  \n\"persona\" : \"Engineering Manager\" ,  \n\"preferences\" : {  \n\"communication_style\" : \"concise\" ,  \n\"detail_level\" : \"high\" ,  \n\"explanations\" : \"include rationale\"  \n} ,  \n\"constraints\" : {  \n\"pii_logging\" : false ,  \n\"max_experimentation_level\" : \"medium\"  \n} ,  \n\"long_term_goals\" : [  \n\"Increase team velocity\" ,  \n\"Reduce incident rate\"  \n]  \n}\n```\nPrompt integration\n ... \nSection Title: ... > Pattern 6 \u2014 Procedural Memory: Evolving Playbooks & ACE\nContent:\nRecent work on Agentic Context Engineering (ACE) frames procedures as *evolving playbooks* that are refined via execution feedback, not just manually curated prompts.\n ... \nSection Title: Agentic Memory Patterns & Context Engineering \u2014 The Real OS of AI Agents > 5.1 Components\nContent:\nLLM Core\nOrchestrator / Agent Runtime (LangGraph, MS Agent Framework, Autogen, custom, etc.)\nWorking Memory\nCurrent context window (system prompt + recent messages + task data).\n4. **Short-Term Store**\nRolling conversation summaries\nScratchpad (transient)\n5. Long-Term Store\nEpisodic log + semantic index (vector DB)\nProfile & preference store (relational / document DB)\nPlaybook store (procedural memory)\n6. **External Tools & Data**\nAPIs, databases, search engines, enterprise systems.\n**7. Observability & Evaluation**\nLogs, metrics, traces, memory audits.\n\u201cThe diagram below shows how these memory layers connect around a single LLM agent.\u201d\nPress enter or click to view image in full size\nLayered Memory for Single Agent\nSection Title: Agentic Memory Patterns & Context Engineering \u2014 The Real OS of AI Agents > 5.2 Flow\nContent:\nRequest comes in (user or system).\nOrchestrator:\nLoads profile memory (Pattern 5).\nRetrieves episodic memories relevant to the user/task (Pattern 4).\nLoads applicable playbooks (Pattern 6).\nBuilds working context using context engineering principles (section 3).\n3. LLM runs, using a scratchpad (Pattern 3) and structured outputs.\n4. Orchestrator:\nLogs this as a new episode.\nUpdates rolling summary (Pattern 2).\nOptionally updates playbooks via curator agent (Pattern 6).\n ... \nSection Title: ... > 6.1 Customer Support Agent\nContent:\nPatterns to use\nP1: Context Framing & Role Scaffolding\nP2: Sliding Window + Rolling Summary\nP4: Episodic Log + Semantic Index\nP5: Profile & Preference Store\nP6: Procedural Playbooks (refunds, upgrades, incidents)\nRecipe\nDesign the system prompt with:\nRole: \u201cSupport agent for PRODUCT X\u201d\nPolicies: escalation, refunds, PII, compliance.\nMemory usage hints: \u201cPrefer user profile for tone; refer episodic memory to recall past issues.\u201d\nImplement rolling summary:\nSummarize long chats into:\n`problem_description` , `steps_taken` , `outcome` , `user_sentiment` .\nUse this summary plus last 5\u201310 messages in context.\nLog episodes & build semantic index:\nStore each conversation and classification tags ( `product` , `error_code` , `resolution_type` ).\nOn new tickets, retrieve similar past episodes and compress them into \u201cWhat worked last time\u201d.\nProfile store:\nKeep preferences like \u201cprefers email follow-up\u201d, \u201cVIP tier\u201d, \u201cpast issues with billing\u201d.\nProcedural playbooks:"
      ]
    }
  ],
  "warnings": null
}
```
